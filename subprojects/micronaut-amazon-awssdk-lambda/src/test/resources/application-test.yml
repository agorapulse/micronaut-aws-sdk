localstack:
  services: lambda,cloudwatchlogs
  shared: true
  env:
    PROVIDER_OVERRIDE_LAMBDA: asf
    DEBUG: 1

# tag::configuration[]
aws:
  lambda:
    functions:
      hello:                          # <1>
        function: HelloFunction       # <2>
# end::configuration[]
